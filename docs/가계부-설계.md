# 💳 가계부 별도 페이지 — 설계서

작성 2026-08-15 · 상태: **구현 완료(배포 전)** · 대상 `money/index.html` (신규) + `index.html` (수정)

> 사장님 결정 2026-08-15 — ①부모방 칩은 남기되 누르면 새 페이지로(옛 화면 삭제) ②보고서는 앱 자동 계산 + 「담당에게 더 자세히」 버튼.
> 아래 §6 질문은 그 결정대로 반영되었다. 남은 일은 **폰에서 확인 → `git push github main`** 뿐이다.

---

## 0. 요청 정리 (사장님 지시 4건)

| # | 지시 | 설계 반영 |
|---|---|---|
| 1 | 가계부는 ⚙️ 옆 **아이콘으로 남기고** 별도 웹페이지로 분리 | `money/` 신규 페이지 + 상단 💳 버튼 (§2) |
| 2 | **기존 것을 활용**하고 보완할 것 | 기존 6뷰 코드를 그대로 이사 + 실측 결함 3건 수정 (§5) |
| 3 | **월 보고서 · 현재 보고서** — 로드맵 보고서 형태로 | 📄 보고서 탭 신설 (§4) |
| 4 | 주로 폰이지만 **폰용·웹용 둘 다** | 한 파일 반응형 3단 (§3) |

---

## 1. 지금 상태 (실측)

가계부는 `index.html` 안 **부모방 → 💳 가계부** 안에 들어 있다.

| 무엇 | 위치 | 크기 |
|---|---|---|
| 화면(템플릿) | `index.html` 1485~2023 | 약 540줄 |
| 로직 | `index.html` 4153~4447 | 약 295줄 |
| 부모방 메뉴 칩 | `index.html:3939` | `{id:'money',label:'💳 가계부'}` |

**기존 6개 뷰** (`index.html:4159`) — 이걸 버리지 않고 그대로 가져간다:

📋 내역 · 📊 분석 · 🗓️ 달력 · 🔁 고정비 · 🎯 예산 · 🏦 자산

**데이터 모델** (`index.html:2935~2936`, 전부 `family_state` 한 행 안):

```
tx[]        {id, d:'YYYY-MM-DD', ty:'in|out|tr', a:금액, c:분류, memo, pay:'bank|card|cash', photo}
budgets[]   {id, name, kind:'in|m|g', c:분류, a:금액, pay:'card|bank|""(미정)', day:'1~31|""', memo}
            └ kind 'in'(입금)·pay·day·memo 는 2026-08-15 예산표 개편에서 추가. 옛 항목은 칸이 없어도 그대로 읽힌다(미정·날짜없음)
fixed[]     {id, name, a, freq:'w|m|q|h|y', day, wd, mo, c, pay, vary, stop, keepM}
            └ stop:'YYYY-MM' = 이 달부터 안 나감(자동). 지우는 게 아니라 그 달부터 목록·합계·보고서에서만 빠진다
              keepM:'YYYY-MM' = 손으로 「다시 넣기」 한 달. 자동판정 시계를 되돌린다 (없으면 되살리자마자 또 빠진다)
              둘 다 2026-08-15 추가 — 판정은 money/index.html 의 autoStopFixed()
fixedPaid{}  {'YYYY-MM': {고정비id: {a:실제낸금액, d:날짜}}}
accounts[]  자산(통장·카드·현금)
installBuys{} 할부로 산 물건. {'롯데하이마트할부#36': '냉장고'} — 열쇠는 상호+총회차 (2026-08-15 추가)
CATS        식비·생활/마트·교육·교통·의료·쇼핑·문화/여가·여행·이체·기타   (index.html:2902)
```

→ **데이터는 손대지 않는다.** 페이지만 옮기므로 기존 기록이 그대로 보인다.

---

## 2. 진입 방식 — cook/·diet/와 똑같이

이미 검증된 패턴이 프로젝트에 두 번 있다(`cook/`, `diet/`). 세 번째로 같은 길을 쓴다.

**새 파일** `money/index.html` — 머리 4줄은 `cook/index.html:7~10`과 동일:

```html
<script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
<script src="../config.js"></script>
<script src="../store.js"></script>
<script src="../support.js"></script>
```

**버튼** `index.html:76~85` 의 ⚙️ 옆, 🍳🥗 와 같은 줄에 한 칸 추가:

```
[⚙️] [💳] [🍳] [🥗] [프로필 전환]
```

- 조건: `showWallet = u.role==='parent'` (엄마·아빠방에서만 — 🍳와 동일 규칙)
- 동작: `goWallet = () => location.href='money/'`
- 돌아오기: 새 페이지 우상단 `🏡 가족 다이어리` (cook 페이지와 같은 버튼)

> ⚠️ **함정 하나 — 이름이 이미 쓰이고 있다.** `index.html:3797`의 `V.goMoney`는 **아이 용돈기입장**으로 가는 기존 함수다. 새 버튼을 `goMoney`로 지으면 아이 화면이 깨진다. 그래서 `goWallet`으로 짓는다.

**부모방 안의 💳 가계부 칩은?** → §6 질문 A

---

## 3. 폰용 · 웹용 (사장님 지시 4)

`cook/`·`diet/`는 `max-width:430px` 폰 전용이다. 가계부는 표·차트·보고서가 있어 넓은 화면에서 그 폭이면 낭비다. **파일 하나 + 미디어쿼리**로 처리한다(폰용 웹용 따로 만들지 않음 — 두 벌은 반드시 어긋난다).

| 화면 | 폭 | 배치 |
|---|---|---|
| 📱 폰 (기본) | ~600px | 1열. 지금과 같은 세로 스크롤, 하단 탭바 |
| 📲 태블릿 | 601~1023px | 2열 카드 그리드 |
| 💻 PC | 1024px~ | 좌측 고정 메뉴(8개 뷰) + 우측 본문 2~3열, 전체 max-width 1280px |

공통 규칙 (전역 CLAUDE.md §6):
- `word-break:keep-all` · 본문 line-height 1.6~1.8 · Pretendard
- 터치 영역 최소 44×44px (폰에서 숫자 입력이 많은 화면이라 특히)
- 금액은 천단위 콤마 + `원`, 표 숫자는 `tabular-nums`
- 표·차트는 감싼 div에 `overflow-x:auto`
- 마이너스는 유니코드가 아닌 `−` 대신 기존 코드와 동일하게 유지 (기존 `txSign`이 `−` U+2212를 쓰고 있어 **HTML 규칙에 맞게 하이픈으로 교체**)
- `@media print` — 월 보고서를 종이로 뽑을 수 있게 (배경 제거·버튼 숨김·`break-inside:avoid`)

---

## 4. 📄 보고서 — 로드맵 형태 (사장님 지시 3)

### 4-1. 어떤 형태인가 (로드맵 실물 기준)

로드맵의 보고서는 두 겹이다. **둘 다 가져온다.**

**① 보고서함 UI** (`로드맵/app/브라보 대시보드.dc.html:1375~1530`)

```
📚 보고서함
┌──────────────────────────────────────────┐
│ [보고서] #012        ★★★★☆    8월 15일 │
│ 2026년 7월 결산 보고서                    │
│ 지출 2,140,000원 · 예산 대비 +12% 초과   │
│                        [✓ 확인] [🗑]     │
└──────────────────────────────────────────┘
```
- **번호**(#012) — 사장님이 "12번 보고서" 로 부를 수 있게
- **★ 중요도 1~5** — 사람이 누르는 곳이 아니라 **보고서가 스스로 매긴 값** (로드맵과 동일 원칙)
- **✓ 확인 → 확인함**, **🗑 → 휴지통** (본문은 안 지우고 표시 상태만 따로 기록)

**② 본문 문체** (`로드맵/app/data/보고서/직원_경리 담당.md` 실물)

```
[확인된 것 — 7월은 예산을 넘겼습니다]
※ 지출 2,140,000원 | 예산 1,900,000원 | 초과 240,000원 (12.6%)

| 분류 | 금액 | 전월비 |
| 식비 | 780,000원 | ▲18% |
...
◐ 고정비 1,180,000원 / 변동비 960,000원

→ 판단: 초과분의 71%가 식비 한 칸에서 나왔습니다.

[주의 신호]
...

▶ 이번 달 실행: ① 식비 예산 70만 → 85만으로 현실화  ② 미납 고정비 2건 정리
```

기호 규칙: `[소제목]` · `※` 핵심수치 · `◐` 비교 · `→` 판단 · `▶` 실행

### 4-2. 월 보고서 (마감본)

- **언제**: 매달 1일 첫 접속 시 지난달분 자동 생성 (+ 아무 달이나 「이 달 보고서 만들기」 수동)
- **한 번 만들면 보고서함에 남는다** — 나중에 과거 달끼리 비교 가능
- **내용** (전부 `tx`·`budgets`·`fixed`·`fixedPaid`에서 계산, 추측 없음):

| 블록 | 담는 것 |
|---|---|
| 결산 요약 | 수입 · 지출 · 순액 · 전월 대비 |
| 어디에 썼나 | 분류별 TOP 5 + 각 전월비 (▲빨강/▼파랑) |
| 고정비 이행 | 해당 N건 중 M건 완료 · 미납 목록 · 예상액 대비 실제 차액 |
| 예산 성적표 | 예산별 달성/초과 · 초과 원인 1위 |
| 결제수단 | 🏦통장 / 💳카드 / 💵현금 비중 |
| 주의 신호 | 평소(직전 3개월 중앙값) 대비 1.5배 넘는 지출 건 |
| ▶ 실행 | 자동 도출 3개 이내 (예: 반복 초과 예산 현실화, 미납 고정비) |

### 4-3. 현재 보고서 (실시간)

- **언제**: 보고서 탭 열면 **항상 지금 값으로 다시 계산** (저장 안 함)
- 「지금 상태 저장」을 누를 때만 스냅샷으로 보고서함에 남음
- **내용**:

| 블록 | 담는 것 |
|---|---|
| 이번 달 진행 | 오늘이 N일차(전체 M일) · 경과율 대비 **지출 페이스** ("이 속도면 월말 예상 ○○원") |
| 남은 돈 | 예산별 잔액 · 소진 예상일 |
| 곧 나갈 돈 | 미납 고정비 D-day 순 · 합계 |
| 이번 주 | 최근 7일 지출 · 직전 7일 대비 |
| ▶ 지금 할 일 | 오늘 기준 실행 3개 이내 |

### 4-4. 저장 위치

`family_state` 문서에 칸 하나 추가 (다른 앱과 같은 방식):

```
moneyReports[]  {id, no:12, kind:'month'|'now', ym:'2026-07', date:'2026-08-01',
                 title, stars:1~5, summary, body, mark:{ok:false, trash:false}}
```

> **AI 없이 동작한다.** 계산과 문장 조립 모두 규칙 기반(로컬)이라 `/family-diary` 담당을 부르지 않아도 즉시 나온다. 캡처 자동 정리(기존 AI 기능)는 지금처럼 담당 호출 방식 그대로 둔다.

---

## 5. 이사하면서 고칠 것 (기존 코드 실측 결함)

「보완할 부분」 — 지금 코드를 읽다가 실제로 발견한 것만 적는다. 없는 문제는 만들지 않는다.

| # | 문제 | 근거 | 조치 |
|---|---|---|---|
| 1 | `goMoney` 이름이 아이 용돈기입장에 이미 쓰임 | `index.html:3797` | 새 버튼은 `goWallet` |
| 2 | **예산 뷰만 이번 달 고정** — 월을 옮겨도 예산 진행률은 항상 이번 달을 본다 | `index.html:4211~4213` `const curM=t.slice(0,7)` | 보고 있는 달(`budM`) 기준으로 수정. 과거 달 보고서와 숫자가 어긋나는 원인 |
| 3 | 마이너스에 유니코드 `−`(U+2212) 사용 | `index.html:4175` | 전역 규칙대로 하이픈으로 |

그 외 동작하는 부분은 **손대지 않는다.**

---

## 6. 사장님이 정하실 것 2가지

**A. 부모방 안의 「💳 가계부」 칩을 어떻게 할까요?**
- (a) **누르면 새 페이지로 이동** — 칩은 남고, 옛 화면 코드 835줄은 삭제 ← 권장
- (b) 칩까지 없애기 — 부모방은 자녀·스티커·설정만
- (c) 옛 화면도 그대로 두기 — 같은 걸 두 곳에서 고쳐야 해서 어긋납니다(비권장)

**B. 보고서를 누가 씁니까?**
- (a) **앱이 자동 계산** — 즉시, 공짜, 숫자는 정확. 문장은 정해진 틀 ← 권장
- (b) 클로드 담당(`/family-diary`)이 작성 — 문장이 살아있지만 사장님이 불러야 나옴
- (c) (a)로 만들고, 원할 때만 「담당에게 더 자세히」 버튼

---

## 7. 만드는 순서 (승인 후)

```
1. money/index.html 껍데기 + 상단 💳 버튼      → 검증: 폰에서 눌러 페이지가 열리고 🏡로 돌아온다
2. 기존 6뷰 이사 (템플릿 540줄 + 로직 295줄)   → 검증: 기존 내역·고정비·예산 숫자가 이사 전과 같다
3. §5 결함 3건 수정                            → 검증: 지난달로 옮겨도 예산 진행률이 그 달 기준
4. 📄 보고서 탭 (현재 → 월 순서로)             → 검증: 7월 보고서 숫자를 내역 합계와 대조
5. 반응형 3단 + @media print                   → 검증: 폰·1024px·인쇄 미리보기
6. index.html 옛 화면 정리 (질문 A 결과대로)   → 검증: 부모방 다른 탭 이상 없음
7. README 파일표 갱신 + GitHub push (배포)     → 검증: 폰에서 공개 주소 확인
```

배포는 `git push github main` 까지 해야 폰에 반영된다 (README:49~55).

---

## 8. 🎯 예산 → 「예산표」 개편 (2026-08-15, 사장님 지시)

> "클릭 방식으로 해 … 되도록 기본 계정은 니가 추가해. 카드인지 통장(현금)인지 미정인지 구분 …
> 지출이 나갈 날짜도 (선택) … 입금·지출(계정별)을 한곳에 깔끔하게 … 동그라미 그래프로도"

**손으로 적는 곳은 두 군데만 남았다** — 목록에 없는 항목(✏️ 직접 적기)과 메모. 나머지는 전부 누른다.

| 무엇 | 어떻게 |
|---|---|
| 기본 계정 | `BPRE` 상수 — 입금 4개 · 매달 지출 10개 · 목표 5개. **이미 담은 것은 목록에서 빠진다**(중복 방지) |
| 종류 | 💵 입금(`kind:'in'` 신설) · 💳 매달 지출(`m`) · 🎯 목표·저축(`g`) |
| 금액 | `+1만/+5만/+10만/+50만/+100만` 을 눌러 더한다 · 지우기 · 직접 적기 칸도 남겨 둠 |
| 결제 방법 | 💳 카드 / 🏦 통장(현금) / ❓ 미정 — **미정이 기본값**(`pay:''`). 입금에는 카드 칸을 안 보여준다 |
| 지출일 | `day` 선택 — 「날짜 미정」 + 1~31일. 안 골라도 된다 |
| 한곳에서 관리 | 예산표 카드(들어올·나갈·남는 돈) → 💵 입금 → 💳 매달 지출(계정별) → 🎯 목표 순서 |
| 동그라미 | `conic-gradient` 한 줄(`V.pieCss`). **SVG 아님** — 이 화면틀은 SVG 네임스페이스를 안 만들어 준다 |

**지켜야 할 것**
- 예산의 `c`(분류)는 **내역(tx)의 분류와 같은 말이어야** 진행률이 맞는다. 그래서 기본 계정은 CATS 10개에 맞춰 두었다.
- 동그라미 색 `PIEC` 8칸은 **명도·채도·색맹 구분·대비 검사를 통과한 조합**이다. 순서를 섞거나 색을 늘리지 마라(9번째부터는 '그 외' 회색으로 접는다).
- 입금 항목에는 진행률 막대가 없다. 입금은 분류로 가려낼 수 없어 **그룹 머리글에서 계획 vs 실제**로만 비교한다.
- 옛 예산 항목은 `pay`·`day`·`memo` 칸이 없다 → 미정·날짜없음·메모없음으로 그대로 읽힌다. 자료 이사(마이그레이션) 불필요.

**검증한 방법** (브라우저 없이 못 믿을 화면이라 실제로 돌려 봤다)
1. `renderVals()`를 노드에서 그대로 실행 — 담기·고치기·지우기·중복 방지·백분율까지 확인
2. 헤드리스 크롬으로 실물 렌더 — 폰 390px / PC 1180px / 담기 폼
3. ⚠ 이 PC의 헤드리스 크롬은 `--window-size` 가 화면 폭에 안 먹는다(항상 485~500). 폭을 바꿔 보려면
   `html{width:390px}` 를 넣고 **미디어쿼리 기준선을 임시로 바꿔** 켜야 한다
