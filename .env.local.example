# 이 파일을 복사해서 이름을 .env.local 로 바꾸고, 값을 채우세요.
# .env.local 은 git에 올라가지 않습니다 (.gitignore에 들어 있음).
#
# 앱에 로그인할 때 쓰는 가족 계정과 같은 이메일·비밀번호입니다.
# VS코드 담당(/family-diary)이 이 계정으로 접속해 부탁을 처리합니다.

FAMILY_EMAIL=가족계정@example.com
FAMILY_PASSWORD=여기에비밀번호
