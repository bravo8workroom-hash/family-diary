# 마인드맵 호출장 — 2026-08-29 12:13

사장님이 대시보드 마인드맵에서 **family-diary**에게 일을 넘기셨다.

※ 가지를 고르지 않고 부르셨다 — 무엇을 할지는 사장님께 여쭤라.

## 이 프로젝트에 남은 지시 1건

- (대기) 1번 스티커 교환 — `mmtdmu517mdv`

## 끝내고 반드시 할 것

1. `app/data/마인드맵.json`(로드맵 프로젝트)에서 그 가지의 `status`를 `완료`로 바꾸고 `report`에 결과 요약(3~6줄)을 채운다.
2. 나머지 필드(id·kind·parent·title·html·photos·project)는 그대로 되돌려줘라 — 빼먹으면 그 필드가 지워진다.
3. 끝난 가지는 화면에서 연한 핑크로 눕고 제목 앞에 날짜가 붙는다. **이미 끝난 가지는 다시 읽지 마라**(사장님 지시).